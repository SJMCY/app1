rror("Failed to get history items");const P={isRefetching:E,hasPageError:A,hasNextPage:k,fetchNextPage:()=>{i&&u(),m&&w()},refetch:()=>{y&&v(),o&&c()}};return _?(0,s.Z)({},P,{data:null}):(0,s.Z)({},P,{data:[...null!=T?T:[],...null!=I?I:[]]})};var m=r(98283);const w=["data"],v=({workspaceId:e,filterFn:t})=>{var r;const a=y({workspaceId:e,pageSize:n,useErrorBoundary:!1}),{data:o}=a,i=(0,m.Z)(a,w);let l=o;return t&&(l=null==o?void 0:o.filter(t)),(0,s.Z)({data:null==(r=l)?void 0:r.map((e=>({id:null==e?void 0:e.id,label:null==e?void 0:e.url,value:null==e?void 0:e.url})))},i)};var g=r(40540);class T{constructor(){var e=this;this.observer=null,this.workspaceId="",this.abortController=null,this.asyncObserver=null,this.changeWorkspace=e=>{this.workspaceId&&this.unsubscribe(),this.workspaceId=e},this.subscribe=async function({workspaceId:t=e.workspaceId,subscriptionHandler:r}){if(""===t)return;if(e.workspaceId!==t&&e.changeWorkspace(t),e.observer||e.asyncObserver)return;const{asyncObserver:s,abortController:o}=(e=>{const t=new AbortController;return{asyncObserver:u.postmanWebsocketGateway.subscribe({signal:t.signal,subscribeRequest:{method:"post",service:"history",path:`/history/subscribe?workspace=${e}`},subscriptionEvents:["broadcast"],responseFilter:e=>{var t;return null==e||null==(t=e.body)?void 0:t.subscription},broadcastFilter:e=>null==e?void 0:e.sub,unsubscribeRequest:{method:"post",service:"history",path:`/history/unsubscribe?workspace=${e}`}}),abortController:t}})(t);e.abortController=o,e.asyncObserver=s,e.observer=await s,e.abortController=null,(0,l.invalidateCache)([a,t]),e.observer.subscribe({next:e=>{r(e,{workspaceId:t})},complete:()=>{e.unsubscribe()},error:async function(s){e.cleanup(),await e.subscribe({subscriptionHandler:r,workspaceId:t})}})},this.unsubscribe=()=>{var e,t;null==(e=this.abortController)||e.abort(),null==(t=this.observer)||t.unsubscribe(),this.cleanup()},this.cleanup=()=>{this.asyncObserver=null,this.observer=null,this.abortController=null}}}const I=new T,_=e=>{(0,l.updateCache)({key:[a,e],updater:e=>{if(e)return(0,s.Z)({},e,{pages:[{items:[],meta:{nextCursor:null}}],pageParams:[null]})}})},k=(e,t,r)=>{e.length>0&&(0,l.updateCache)({key:[a,t],updater:t=>{var a;if(t)return{pages:null!=(a=t.pages.map((t=>(0,s.Z)({},t,{items:t.items.filter((t=>!e.includes(null!=r&&r.checkModelId?(e=>{const t=e.split("-");return!t||t.length<6?{modelId:e}:{owner:t.shift(),modelId:t.join("-")}})(t.id).modelId:t.id)))}))))?a:[],pageParams:null==t?void 0:t.pageParams}}})},A=["pages"],E=(e,t)=>{const r=e.split("-");return!r||!t||r.length>=6?e:`${t}-${e}`},P=(e,t)=>({id:E(e.id,e.value.owner),method:e.value.method,url:e.value.url,owner:`${e.value.owner}`,createdAt:new Date(e.value.createdAt),team:null,model:"history",type:"request",depth:1,workspace:t,lastRevision:null,cursor:null}),R=(e,{workspaceId:t})=>{const r=[],o=[];for(const s of e.data)switch(s.op){case"create":r.push(P(s,t));break;case"delete":o.push(s.id);break;case"deleteAll":_(t)}r.length>0&&((e,t)=>{e.length>0&&(0,l.updateCache)({key:[a,t],updater:t=>{if(!t)return{pages:[{items:[...e],meta:{nextCursor:null}}],pageParams:[null]};const{pages:[r,...a]}=t,o=(0,m.Z)(t,A),n=[...e,...r.items];return(0,s.Z)({pages:[(0,s.Z)({},r,{items:n}),...a]},o)}})})(r,t),o.length>0&&k(o,t,{checkModelId:!0})};class f{constructor(){var e=this;this.observer=null,this.workspaceId="",this.abortController=null,this.asyncObserver=null,this.changeWorkspace=e=>{this.workspaceId&&this.unsubscribe(),this.workspaceId=e},this.subscribe=async function({workspaceId:t=e.workspaceId,subscriptionHandler:r}){if(""===t)return;if(e.workspaceId!==t&&e.changeWorkspace(t),e.observer||e.asyncObserver)return;const{asyncObserver:s,abortController:a}=(e=>{const t=new AbortController;return{asyncObserver:u.postmanWebsocketGateway.subscribe({signal:t.signal,subscribeRequest:{method:"post",service:"scribe",path:"/subscriptions/",body:{workspace:e}},subscriptionEvents:["multiprotocolHistory"],responseFilter:e=>{var t;return null==e||null==(t=e.body)||null==(t=t.data)?void 0:t.subscription},broadcastFilter:e=>null==    report(context, messages.useCallback, 'useCallback', {
                node,
              });
              break;
            }

            // Storing all functions and methods that contains this.state
            if (current.type === 'MethodDefinition') {
              methods.push({
                methodName: 'name' in current.key ? current.key.name : undefined,
                node,
              });
              break;
            } else if (
              current.type === 'FunctionExpression'
              && 'key' in current.parent
              && current.parent.key
            ) {
              methods.push({
                methodName: 'name' in current.parent.key ? current.parent.key.name : undefined,
                node,
              });
              break;
            }

            // Storing all variables containing this.state
            if (current.type === 'VariableDeclarator') {
              vars.push({
                node,
                scope: getScope(context, node),
                variableName: 'name' in current.id ? current.id.name : undefined,
              });
              break;
            }

            current = current.parent;
          }
        }
      },

      Identifier(node) {
        // Checks if the identifier is a variable within an object
        /** @type {import('eslint').Rule.Node} */
        let current = node;
        while (current.parent.type === 'BinaryExpression') {
          current = current.parent;
        }
        if (
          ('value' in current.parent && current.parent.value === current)
          || ('object' in current.parent && current.parent.object === current)
        ) {
          while (current.type !== 'Program') {
            if (isFirstArgumentInSetStateCall(current, node)) {
              vars
                .filter((v) => v.scope === getScope(context, node) && v.variableName === node.name)
                .forEach((v) => {
                  report(context, messages.useCallback, 'useCallback', {
                    node: v.node,
                  });
                });
            }
            current = current.parent;
          }
        }
      },

      ObjectPattern(node) {
        const isDerivedFromThis = 'init' in node.parent && node.parent.init && node.parent.init.type === 'ThisExpression';
        node.properties.forEach((property) => {
          if (
            property
            && 'key' in property
            && property.key
            && 'name' in property.key
            && property.key.name === 'state'
            && isDerivedFromThis
          ) {
            vars.push({
              node: property.key,
              scope: getScope(context, node),
              variableName: property.key.name,
            });
          }
        });
      },
    };
  },
};
